
// src/services/AnalysisManager.test.ts
import { App } from 'obsidian';
import { AIServiceError } from '../utils/error';

describe('AnalysisManager', () => {
    test('placeholder test', () => {
        expect(true).toBe(true);
    });
});

export {}; // This makes the file a module